def extracting_data(number: int):
    for _ in range(number):
        ranges = input().split("-")
        for i in ranges:
            start = i[0]
            end = i[1]



num = int(input())
print(extracting_data(num))
# print(extracting_data(num))
